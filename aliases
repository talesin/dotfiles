alias cls='if [ -n "$ZELLIJ" ]; then zellij action clear; else clear; fi'
alias zka="zellij ls | grep EXITED | awk '{print $1}' | sed -e 's/\x1B\[[0-9;]*[mG]//g' | xargs -I % zellij d %"
#alias claude="$HOME/.claude/local/claude"
