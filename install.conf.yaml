- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.byobu: byobu/
    ~/.gitconfig: gitconfig
    ~/.spacemacs: spacemacs
    ~/.zprezto: zprezto

- shell:
  - [git submodule update --init --recursive, Installing submodules]
